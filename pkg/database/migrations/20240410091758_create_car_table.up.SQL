CREATE TABLE cars (
    regNum TEXT NOT NULL PRIMARY KEY,
    mark TEXT NOT NULL,
    model TEXT NOT NULL,
    year INTEGER NOT NULL,
    owner_name TEXT NOT NULL,
    owner_surname TEXT NOT NULL,
    owner_patronymic TEXT NOT NULL
);